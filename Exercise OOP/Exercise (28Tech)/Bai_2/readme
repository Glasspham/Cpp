Bài 2. Tính tổng 2 phân số
Viết chương trình xây dựng cấu trúc Phân số gồm hai phần tử là tử số và mẫu số. Các giá
trị đều nguyên dương và không quá 9 chữ số.
Sau đó thực hiện nhập vào hai phân số p và q. Tính tổng p + q, rút gọn và in ra kết quả.
Input
Có bốn số nguyên dương lần lượt là tử số và mẫu số của p rồi đến q.
Output
Ghi ra phân số tổng p + q ở dạng tối giản như trong ví dụ
Ví dụ
Input
123 456 12 34
Output
1609/2584
Hàm main có sẵn như sau:
    int main(){
    struct PhanSo p,q;
    nhap(p); nhap(q);
    PhanSo t = tong(p,q);
    in(t);
    return 0;
}